{# @prop as 'h4' The HTML tag to render. Defaults to `h4` #}
{# @block content The title text of the alert #}
{%- props as = 'h4' -%}

<{{ as }} class="alert-heading {{ attributes.render('class') }}" {{ attributes }}>
	{%- block content %}{% endblock -%}
</{{ as }}>
