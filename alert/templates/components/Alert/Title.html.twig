{# @prop level number Level of the heading. Defaults to 4 #}
{# @block content The title text of the alert #}
{%- props level = 4 -%}

<h{{ level }} class="alert-heading {{ attributes.render('class') }}" {{ attributes }}>
	{%- block content %}{% endblock -%}
</h{{ level }}>
