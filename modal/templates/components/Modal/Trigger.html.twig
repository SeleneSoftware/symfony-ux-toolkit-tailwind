{# @block content The label of the collapsible trigger #}

{%- set trigger_content -%}
	{%- block content %}{% endblock -%}
{%- endset -%}

<twig:Button
	data-bs-toggle="modal"
	data-bs-target="#{{ _modal_id }}"
	{{ ...attributes }}
>
	{{- trigger_content -}}
</twig:Button>
